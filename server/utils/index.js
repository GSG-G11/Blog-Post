const loginSchema = require('./validation/loginSchema');
const signSchema = require('./validation/signSchema');
const blogSchema = require('./validation/blogSchema');

module.exports = {
  loginSchema,
  signSchema,
  blogSchema,
};
